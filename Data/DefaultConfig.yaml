Prefix: .
Status: Reset Mode

Launch Settings:
  Reset Mode: False
  Web Server: False

Roles:
  # < Role ID >: 
  #   - < Permission Key >
  #   - < Permission Key ( If multiple needed ) >

Users:
  # < User ID >:
  #   - < Permission Key >
  #   - < Permission Key ( If multiple needed ) >