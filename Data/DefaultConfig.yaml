Prefix: .
Status: Reset Mode

Launch Settings:
  Reset Mode: False
  Web Server: False

# Permission Setup
Roles:
  # < Role ID >: 
  #   - < Permission Key >
  #   - < Permission Key ( If multiple needed ) >

Users:
  # < User ID >:
  #   - < Permission Key >
  #   - < Permission Key ( If multiple needed ) >

Everyone:
  # - < Permission Key >
  # - < Permission Key ( If multiple needed ) >